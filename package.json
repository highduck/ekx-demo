{
    "name": "@ekx/demo",
    "version": "1.5.16+91",
    "private": true,
    "scripts": {
        "postinstall": "cmake-build",
        "build:web": "ekx web clean --verbose",
        "build:android": "ekx android bundle clean --verbose",
        "build:assets:dev": "ekx assets --dev -v",
        "clean": "rimraf ./build ./export ./.firebase",
        "run-web": "ekx web -v --run",
        "gen-api": "node scripts/gen.js",
        "release-patch-web": "ekx web clean --bump patch --deploy internal -v"
    },
    "dependencies": {
        "@ekx/appbox": "0.0.8",
        "@ekx/cli": "0.1.13",
        "@ekx/nep": "0.0.2",
        "@ekx/pocketmod": "0.0.1",
        "@ekx/quickjs": "0.0.1",
        "cmake-build": "0.0.5"
    },
    "devDependencies": {
        "rimraf": "^3.0.2"
    }
}
