{
  "name": "@ekx/demo",
  "version": "1.1.8",
  "private": true,
  "scripts": {
    "postinstall": "npm-cmake",
    "build:web": "ekx web clean --verbose",
    "build:android": "ekx android bundle clean --verbose"
  },
  "dependencies": {
    "@ekx/cli": "link:../ekx/cli",
    "@ekx/ekx": "link:../ekx",
    "npm-cmake": "0.0.3"
  }
}
