{
    "name": "@ekx/demo",
    "version": "1.5.19+94",
    "private": true,
    "type": "module",
    "scripts": {
        "postinstall": "ekx cmake",
        "build:web": "ekx web clean --verbose",
        "build:android": "ekx android bundle clean --verbose",
        "build:assets:dev": "ekx assets --dev -v",
        "run-web": "ekx web -v --run",
        "gen-api": "node scripts/gen.js",
        "deploy:web-patch": "ekx web clean --bump patch --deploy internal -v"
    },
    "dependencies": {
        "@ekx/ekx": "1.0.13"
    },
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=7.0.0"
    }
}
