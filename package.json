{
  "name": "@ekx/demo",
  "version": "1.1.15",
  "private": true,
  "scripts": {
    "postinstall": "npm-cmake",
    "build:web": "ekx web clean --verbose",
    "build:android": "ekx android bundle clean --verbose"
  },
  "dependencies": {
    "@ekx/cli": "0.1.1",
    "@ekx/ekx": "0.1.1",
    "npm-cmake": "0.0.5"
  }
}
