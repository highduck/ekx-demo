{
  "name": "@ekx/demo",
  "version": "1.1.20",
  "private": true,
  "scripts": {
    "postinstall": "npm-cmake",
    "build:web": "ekx web clean --verbose",
    "build:android": "ekx android bundle clean --verbose",
    "build:assets:dev": "DEV_ASSETS=1 ekx assets"
  },
  "dependencies": {
    "@ekx/cli": "0.1.3",
    "@ekx/appbox": "0.0.2",
    "npm-cmake": "^0.0.10"
  }
}
